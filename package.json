{
  "name": "laskuri_backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "lint": "eslint .",
    "test": "cross-env  NODE_ENV=test node --test",
    "start:test": "NODE_ENV=test node index.js",
    "build:ui": "rm -rf dist && cd ../laskuri && npm run build && cp -r dist ../laskuri_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.21.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^6.13.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.8.0",
    "@flydotio/dockerfile": "^0.5.0",
    "@stylistic/eslint-plugin-js": "^1.8.1",
    "cross-env": "^7.0.3",
    "eslint": "^9.8.0",
    "globals": "^15.9.0",
    "nodemon": "^3.1.7",
    "supertest": "^7.0.0"
  }
}
